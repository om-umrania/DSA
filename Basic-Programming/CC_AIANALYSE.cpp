#include <bits/stdc++.h>
using namespace std;

int main() {
    int c;
    cin>>c;
        if (c >=1 && c <= 10000)
            cout<<"YES"<<endl;
        else
            cout<<"NO"<<endl;
    return 0;
}
